#![cfg(feature = "poseidon-precompile")]

#[test]
#[ignore = "perf-style placeholder; run manually once poseidon benchmark harness is wired"]
fn poseidon_precompile_vs_software_perf_placeholder() {
    // Placeholder module so the perf suite compiles and formats when the feature is enabled.
}
