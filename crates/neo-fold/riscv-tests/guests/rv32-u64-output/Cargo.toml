[package]
name = "rv32-u64-output"
version = "0.0.0"
edition = "2021"
publish = false

[[bin]]
name = "rv32_u64_output"
path = "src/main.rs"

# Standalone guest project: do not inherit the repo workspace.
[workspace]

[profile.release]
panic = "abort"

[dependencies]
nightstream-sdk = { path = "../../../../nightstream-sdk", default-features = false }
